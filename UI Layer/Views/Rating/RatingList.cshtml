@model List<UI_Layer.Models.Rating>

<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Recipe List</title>
	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
	<style>
		body {
			padding-top: 56px; /* Adjust for fixed navbar */
		}

		.footer {
			background-color: #f8f9fa;
			padding: 20px;
			text-align: center;
		}

		.navbar-brand {
			font-weight: bold;
		}
	</style>
</head>
<body>
	<div class="container mt-5">
		<h3 class="mt-4 mb-4">All Rating List</h3>

		<!-- Add New User Link -->
		<div class="mb-4">
			<a asp-route="AddRating" class="btn btn-primary">Add New Rating</a>
		</div>
		<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
			<div class="container">
				<div class="collapse navbar-collapse" id="navbarNav">

					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link" asp-route="RecipeStepList">Recipe Step List</a>
						</li>

						<li class="nav-item">
							<a class="nav-link" asp-route="IngredientList">Ingredient List</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" asp-route="CommentList">Comment List</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" asp-route="RecipeList">Recipe List</a>
						</li>
						
						<li class="nav-item">
							<a class="nav-link" asp-route="Dashboard">Home</a>
						</li>
					</ul>
				</div>
			</div>
		</nav>
	</div>
	<div class="container mt-5">
		<table class="table table-striped table-bordered">
			<thead class="thead-light">
				<tr>
					<th>Rating ID</th>
					<th>Rating Star</th>

					<th>User Id</th>
					<th>Recipe Id</th>
				</tr>
			</thead>
			<tbody>
				@{
					@if (Model != null && Model.Any())
					{
						foreach (var rate in Model)
						{
							<tr>
								<td>@rate.RatingId</td>
								<td>@rate.Star</td>
								<td>@rate.UserId</td>


								<td>@rate.RecipeId</td>

								<td><a asp-route="GetRatingtById" class="btn btn-sm btn-info" asp-route-id="@rate.RatingId">Details</a> </td>
								<td><a asp-route="DeleteRating" class="btn btn-sm btn-danger" asp-route-id="@rate.RatingId">Delete</a> </td>
								<td><a asp-route="EditRating" class="btn btn-sm btn-warning" asp-route-id="@rate.RatingId">Edit</a> </td>
							</tr>
						}
					}
					else
					{
						<tr>
							<td colspan="6">No Rating found.</td>
						</tr>
					}
				}
			</tbody>
		</table>
	</div>
	<!-- Footer -->
	<footer class="footer mt-5">
		<div class="container">
			<p>&copy; 2024 RetingList Dashboard. All rights reserved.</p>
		</div>
	</footer>
</body>

</html>

